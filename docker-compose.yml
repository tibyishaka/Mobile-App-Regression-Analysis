version: '3.8'

services:
  salary-prediction-api:
    build: .
    container_name: salary-prediction-api
    ports:
      - "8000:8000"
    environment:
      - DEBUG=False
      - LOG_LEVEL=INFO
    volumes:
      - ./API:/app
      - ./Linear_Regression:/app/../Linear_Regression
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - salary-api-network

networks:
  salary-api-network:
    driver: bridge
